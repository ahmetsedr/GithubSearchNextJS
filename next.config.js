/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
          domains: ['via.placeholder.com'], // Add the domain(s) causing the error
        }
}

module.exports = nextConfig
